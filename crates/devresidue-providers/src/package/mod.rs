//! Package manager providers — **skeleton (Phase 8)**.
//!
//! Reserved for package-store discovery that is not developer-cache shaped
//! (`node_modules`, `.venv`, language-specific stores referenced from
//! projects). See also `dev_cache/` and `project/`. Exact module split is
//! settled when the Phase 8 provider list is implemented.

// No items yet by design.
